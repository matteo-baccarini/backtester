{
  "name": "@backtester/strategy-engine",
  "version": "0.1.0",
  "description": "Core backtesting logic and technical indicators",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@backtester/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
